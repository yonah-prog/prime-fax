[build]
builder = "nixpacks"

[build.nixpacksConfig]
providers = ["node"]

[variables]
NODE_VERSION = "20"

[deploy]
startCommand = "npm run db:migrate && next start -p ${PORT:-3000}"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
